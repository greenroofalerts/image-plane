.venv/
bin/
# but keep the Cloud Agent dev-environment config (.cursor/bin/sips shim)
!.cursor/bin/
__pycache__/
*.egg-info/
.pytest_cache/
image_plane.db
tests/fixtures/generated/

# Real photos / page screenshots must NEVER be committed (repo rule) — they live in
# ~/leeos-private/. This blocks the whole class, not just one folder.
*.png
*.jpg
*.jpeg
*.heic
*.HEIC
*.JPG
