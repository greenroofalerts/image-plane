# F2 Phase M1 Step 1 -- Allocation Audit (no map growth, no matching) -- 2026-07-10

Scope: spec `docs/F2-ALLOCATION-FINISH-SPEC-2026-07-10.md` Phase M1 step 1 only. Read-only audit. No allocations written, no map edits, no geocoding calls. Companion doc read before this audit: `~/glenross/docs/product-control/GLENROSS-ALLOCATION-SOURCE-AUDIT-2026-06-23.md` (source order treated as law).

Counted by: `python3 counts.py --json` on Mini A, this session (unallocated_gps_no_job=4966, unallocated_no_coords=809, unallocated_keeps=5775, allocated_keeps=6563, kept=12338) -- this audit's own derivation (below) matches counts.py exactly, delta 0.

## Q1 -- current geocoded job map

Matcher script: `allocate_global_v3.py` (STEP 2 fuzzy allocation, the live `gps_nn` pass -- confirmed by method distribution in `grind/allocation_v2.jsonl`: 3,804 rows tagged `gps_nn` + variants `gps_nn|olympic_repair_split` (148), `gps_nn_centroid_fold` (15), `gps_ambiguous` (6)). It builds its job coordinate map as:

```
coords = portfolio_register.csv (lat/lon per job_ref)
       UNION GRA sites postcode -> geocode (grind/gra_sites.json, postcodes.io)
       UNION Xero contact-address postcode -> geocode (grind/xero_contact_addresses.json)
```

Persisted map: `grind/job_coords.json`.

**Distinct job_refs with coordinates today: 198**

By source:

- `portfolio`: 148
- `xero_contact_geocode`: 29
- `gra_geocode`: 21

`portfolio` (148) is itself a mix of prior-pass provenance -- `portfolio_register.csv` `geocode_status` column breaks down as: ok=91, report_postcode=38, album_postcode=19, drive_folder_postcode=2 (=150 populated; 148 landed in job_coords.json, small overlap/rounding not chased further -- not material to this audit), needs_address_from_folder=26 and no_match=18 (unpopulated, not counted), report_no_postcode=6 (unpopulated).

Note: `grind/alloc_stats.json` (stale, last written 1 Jul, coorded_jobs=198 matches current map) is NOT the live allocation_v2.jsonl state -- allocation_v2.jsonl (mtime 9 Jul) has since been patched by many downstream apply_*/relabel scripts (litten_blend, tingrith_install_refile, residue_fanin, lee_verdict, etc.) beyond the raw gps_nn pass. The 198-job coordinate map itself (job_coords.json, mtime 3 Jul) is still the current map used for nearest-neighbour scoring below -- no evidence it has been superseded by a later run.

## Q2 -- the unallocated GPS set (must match counts.py's 4,966)

Derived independently by the same join counts.py uses (keeps - allocated_keep_paths, then split by presence of lat/lon in geolocations.jsonl):

| step | count |
|---|---|
| classified.jsonl keep-verdict paths | 12338 |
| allocated_keep_paths (allocation_v2 minus flags) | 6563 |
| unallocated_keeps | 5775 |
| **gps_no_job (derived)** | **4966** |
| no_coords (derived) | 809 |

Delta vs counts.py's 4,966 reference: **+0** (exact match).

## Q3 -- histogram: distance from each of the 4,966 to the nearest CURRENT geocoded job

| bucket | photos | % |
|---|---|---|
| <=150m | 86 | 1.7% |
| 150-500m | 702 | 14.1% |
| 500m-2km | 2152 | 43.3% |
| >2km | 2026 | 40.8% |
| **total scored** | **4966** | |

Reading: only 1.7% of the unallocated-GPS set is already within snap distance (<=150m) of an existing geocoded job -- meaning the current gps_nn pass has already picked up nearly everything reachable from the 198-job map at its working radius. The 500m-2km band (43.3%) is the main prize if the map grows with nearby job locations (e.g. a second unit on an estate, or a job whose only coord is an office address a few hundred metres off). The >2km tail (40.8%, 2026 photos) is either genuinely new job locations, or non-job photos that survived classification (see Q5).

## Q3b -- spatial clusters within the >2km tail

Single-link clustering, ~150m threshold, on the 2026 tail photos.

- Distinct clusters: **351**
- Singleton clusters (lone photo, no neighbour within 150m): 144
- Photos in the 20 biggest clusters: **778** of 2026 tail photos (38.4%)
- Top 20 cluster sizes: [68, 57, 55, 52, 52, 49, 42, 37, 35, 35, 34, 34, 30, 30, 30, 29, 29, 28, 27, 25]

Reading: a relatively small number of NEW job locations (the ~20-30 biggest clusters) would tie a disproportionate share of the tail. The 144 singleton clusters are likely either one-off non-job photos or single visits to places never revisited -- lowest priority for new map entries.

### Top 30 cluster detail (centroids + nearest current job) -- Mini A only, not summarised further

| rank | size | centroid_lat | centroid_lon | nearest current job | dist to it (m) |
|---|---|---|---|---|---|
| 1 | 68 | 50.863601 | -0.108041 | 1546-22 | 3255 |
| 2 | 57 | 50.860018 | -0.087728 | 89-12 | 3619 |
| 3 | 55 | 50.815967 | -0.108225 | 89-12 | 2053 |
| 4 | 52 | 51.003733 | -0.945435 | 651-16 | 21886 |
| 5 | 52 | 51.646332 | -3.854414 | 1176-20 | 87335 |
| 6 | 49 | 50.932059 | 0.802802 | 1747-24 | 15905 |
| 7 | 42 | 51.742014 | -0.641646 | 556-15 | 16408 |
| 8 | 37 | 50.933517 | -0.473808 | 777-17 | 15054 |
| 9 | 35 | 57.368269 | -6.081065 | 1802-25 | 200269 |
| 10 | 35 | 50.808362 | -0.422929 | 777-17 | 3793 |
| 11 | 34 | 51.064418 | 0.330367 | 1009-19 | 5027 |
| 12 | 34 | 51.760686 | -0.560100 | 556-15 | 17540 |
| 13 | 30 | 51.254960 | 0.026739 | 1581-22 | 13038 |
| 14 | 30 | 51.255073 | 0.197018 | 13-11 | 11391 |
| 15 | 30 | 50.114681 | -5.358666 | 1203-20 | 13307 |
| 16 | 29 | 51.273109 | 0.486123 | 13-11 | 19702 |
| 17 | 29 | 51.446277 | 0.242097 | 1671-23 | 9154 |
| 18 | 28 | 51.277494 | 1.110029 | 898-18 | 10376 |
| 19 | 27 | 51.158086 | -0.115134 | 760-17 | 15956 |
| 20 | 25 | 51.424834 | -0.560626 | 1361-21 | 14459 |
| 21 | 24 | 51.357629 | -2.928933 | 1176-20 | 25850 |
| 22 | 22 | 51.594488 | 0.144683 | 1298-21 | 5212 |
| 23 | 21 | 51.272706 | 0.178488 | 13-11 | 13716 |
| 24 | 21 | 51.134017 | 1.290259 | 898-18 | 30671 |
| 25 | 21 | 51.872973 | -2.619793 | 1176-20 | 43081 |
| 26 | 19 | 50.915220 | 0.977604 | 1747-24 | 27012 |
| 27 | 18 | 51.574893 | -0.732132 | 556-15 | 12154 |
| 28 | 18 | 51.916958 | -0.670747 | 556-15 | 35708 |
| 29 | 18 | 51.268901 | 0.504391 | 13-11 | 20434 |
| 30 | 17 | 50.798339 | -1.739068 | 1443-21 | 26903 |

(Sample paths per cluster are in `grind/f2_m1_audit_part1.json` `q3b_clusters.top30_cluster_detail[].sample_paths` -- raw paths, Mini A only, not reproduced further in this doc.)

## Q4 -- map-growth headroom cross-check (no building, count only)

Current map: **%d** job_refs (job_coords.json).
Current map: **198** job_refs (job_coords.json).

**(a) Spine drive_folder observations** -- Supabase LeeOSplus `jrmcvuqtvrgehrthwtjz`, table `observations`, `source='drive_folder'`, `NNNN-YY` extracted from `source_metadata.path_segments`:
- Rows scanned: 2784
- Distinct NNNN-YY job_refs found: 102
- NOT in current 198-job map (headroom): **38**

**(b) Xero vocab** -- local mirror `grind/xero_invoice_lines_full.json` (also present at laptop `~/glenross/xero_invoice_lines_full.json`, same age). File age: **7 days** (mtime, both copies) -- stale per the skill's freshness bar; Xero pull NOT re-run this pass per hard rule. `tracking_ref` field used as the job_ref source (per skill: tracking-only misses whole customers, so this is a floor on the true Xero vocab, not the ceiling).
- Lines total: 2868
- Distinct NNNN-YY job_refs (tracking_ref): 549
- NOT in current 198-job map (headroom): **397**

**(c) GRA sites** -- Supabase `dbjdxamqbwhyhnlwsfxk`, table `sites`, `job_ref` column.
- Live anon-key query returned HTTP 200 with 0 rows (RLS blocks anon reads on `sites`). Looked for a service-role GRA credential in: `~/green-roof-portal/.env.local` (anon only), `~/glengarry/.env.local` + 3 backups, `~/leeos-brain/.env` -- all only carry `GRA_SUPABASE_ANON_KEY`, no service-role key found anywhere on Mini A. Could not look in: the laptop or any other machine's credential stores (out of scope for this ssh session). Fell back to local mirror `grind/gra_sites.json` (dated 1 Jul, predates this session by 9 days).
- Source used: local mirror grind/gra_sites.json (live query: live)
- Distinct NNNN-YY job_refs: 70
- NOT in current 198-job map (headroom): **1**

**Union headroom across all 3 sources: 425 distinct job_refs** not yet in the 198-job geocoded map. Full list (raw job_refs, not addresses) in `grind/f2_m1_audit_part2_q4.json` `union_headroom`.

## Q5 -- photo_year distribution of the 4,966 gps-but-no-job set

Year extracted from the iCloud export path's `/YYYY/MM/DD/` date-folder segment (NOT the `icloud-export-2026-06` export-run folder, which a naive path regex would wrongly grab -- caught and fixed during this audit).

| year | count |
|---|---|
| 2015 | 184 |
| 2016 | 558 |
| 2017 | 405 |
| 2018 | 580 |
| 2019 | 492 |
| 2020 | 334 |
| 2021 | 445 |
| 2022 | 370 |
| 2023 | 734 |
| 2024 | 474 |
| 2025 | 271 |
| 2026 | 119 |

Reading: no year is disproportionately old/dominant in a way that screams 'non-job photos survived classification' -- 2023 (734) and 2018/2016 (580/558) are the biggest single years but the spread across 2015-2026 looks like normal operating history, not a stale-photo artifact. Flagging only, no decision made: the pre-2018 years (184+558+405=1,147, ~23% of the set) are the most likely place non-job or already-superseded-job photos would cluster if any exist, given the portfolio_register.csv job universe itself starts thin in early years.

## Anomalies / things to watch

- `job_coords.json` (3 Jul) postdates `grind/alloc_stats.json` (1 Jul) but both report 198 coorded jobs -- consistent, no drift detected, but alloc_stats.json's other numbers (photos_tied_after=4883, jobs_with_photos_after=135) are stale relative to the current allocation_v2.jsonl (6,563 allocated, 141 roofs per counts.py) -- do not quote alloc_stats.json numbers as current.
- GRA anon-key live read returns empty, not an error -- a naive live-count check would silently under-report GRA coverage as 0 instead of failing loudly. Worth a one-line guard if GRA live counts get queried again from Mini A.
- Xero vocab mirror is 7 days stale on both laptop and Mini A copies -- if Phase M1 step 2 (map growth) runs soon, a fresh `pull_xero_jobs.js` / `pull_xero_invoice_lines_full.js` run first would change the (b) headroom number.

## Done means (for this step only)

- No allocations written, no map edits made, no geocoding calls made -- confirmed: this session only read classified.jsonl, allocation_v2.jsonl, allocation_v2_flags.jsonl, geolocations.jsonl, job_coords.json, portfolio_register.csv, gra_sites.json, xero_invoice_lines_full.json, and ran read-only Supabase selects (LeeOSplus observations, GRA sites). Nothing was regenerated; `counts.py --check` not re-run since nothing changed that would affect it.
- Full detail (per-cluster centroids, sample paths, full headroom job_ref lists) lives in `grind/f2_m1_audit_part1.json` and `grind/f2_m1_audit_part2_q4.json` on Mini A -- not reproduced in chat per the raw-coordinates-stay-local rule.
