# HANDOVER-F6 — repo view by billable event: verify-then-report (2026-07-11, ~19:25)

You are the fresh window. Canon promise (state first message): **every roof keeps its
whole life — its photos in time order with Lee's words on them — visible to Lee and,
curated, to the customer.** Read `docs/F6-REPO-VIEW-SPEC-2026-07-11.md` +
`~/leeos-private/HANDOVER-2026-07-11-PHOTO-REPO-VIEW.md` first; Mini A pages are
Lee-internal ONLY (permanent ruling). This window (94% context) spec'd F6, ran three
sonnet doers, verified iteratively, committed `398aa07` + `3af283c` on branch
`f6-repo-view` (NOT merged to main). **Doer self-reports below are TESTIMONY — nothing
is done until YOUR checks pass.** No new Lee rulings were heard in this window
(nothing to ledger; two Lee-questions raised by us, listed at the end).

## Doer A — billable events + sister refs (CLOSED by its own report, unverified here)

Asked: build `grind/billable_events.json` (phase windows per roof, installs never
split, £-redacted) + `grind/sister_refs.json` (confirmed/candidate/same_site tiers,
Olympic Mews acceptance) per spec §Outputs 1–2.
Lands: Mini A `~/image-plane/grind/billable_events.json` + `grind/sister_refs.json`;
script laptop `scripts/mini_a/build_billable_events.py` (committed 3af283c), same file
scp'd on Mini A.
Testimony: 138 roofs / 612 windows (Install 39, Repair 18, Diagnostic 37, Summer 31,
Autumn 67, Spring 126, Winter 6, Visit 288); 9 same_site groups; 44 labels all ≥8
chars; Ainsworth Ave + Victoria Rd groups removed on my order; £ assert passed.

Verify (ssh macminia@192.168.178.61, cd ~/image-plane):
```
python3 - <<'EOF'
import json
be=json.load(open("grind/billable_events.json")); sr=json.load(open("grind/sister_refs.json"))
roofs=[k for k in be if k!="_meta"]; wins=sum(len(be[k]) for k in roofs)
print("roofs",len(roofs),"windows",wins)                       # expect 138 / 612
t=[w["header"] for w in be["1124-19"] if w["phase"]=="Install"]
print("trinity install:",t)                                     # expect ONE: Install — June–November 2021
assert not any("£" in json.dumps(be[k]) for k in roofs), "POUND LEAK"
labels=[(k,tier,e.get("ref"),e.get("label")) for k,v in sr.items() if k!="_meta"
        for tier in ("confirmed","candidate","same_site") for e in (v.get(tier) or [])]
print("labels",len(labels),"weak",[l for l in labels if len((l[3] or ""))<8])  # 44 / []
print("ainsworth residue",[l for l in labels if l[2] in ("453-15","649-16","996-19") or l[0] in ("453-15","649-16","996-19")])  # []
print("olympic 301-14:",[ (e["ref"]) for e in sr["301-14"]["same_site"]])      # 1336-21,673-16,676-16,735-16 (order may differ)
EOF
```

## Doer B — dedupe pass (RUNNING at handover time; false-success already caught once)

Asked: `grind/repo_dedupe_flags.jsonl` per spec §Output 3 — exact/near/possible_twin/
prior_pass; sources never deleted. Round 1 FALSELY reported done with 0/8,326 hashed
(basename collisions in sips --out + lookup mismatch); I sent the fix (unique
path-sha16 symlink names → hash_thumbs2, abort if >2% hash failures). Round 2 was at
thumb batch ~19/41 at 19:19.
Lands: Mini A `grind/repo_dedupe_flags.jsonl`, log `grind/repo_dedupe.log`, thumbs
`grind/hash_thumbs2/`; script `scripts/mini_a/build_repo_dedupe.py` (laptop mirror
NOT yet committed — commit after verifying).
Facts I verified myself this window: exact=0 is TRUE (my own join: 8,488 valid rows,
100% sha256, zero same-roof dupe groups); possible_twin=0 honest (valid set has 0
Takeout rows); 185 valid paths missing on disk excluded with log note (known F8
look-list).

Verify:
```
ssh macminia@192.168.178.61 'tail -20 ~/image-plane/grind/repo_dedupe.log'
# MUST show: "dHash computed for N/8141" with N ≥ 7978 (98%) — 0/N = the known false-success, REJECT
# MUST show: "PHASE 2 NEAR: X pairs … Y photos collapsed" and a DONE line consistent with it
ssh macminia@192.168.178.61 'python3 -c "
import json,collections; k=collections.Counter()
for l in open(\"/Users/macminia/image-plane/grind/repo_dedupe_flags.jsonl\"):
    l=l.strip()
    if l: k[json.loads(l).get(\"kind\")]+=1
print(dict(k))"'
# expect prior_pass=240, exact=0, possible_twin=0, near=<whatever landed>
# Spot-check 2 near pairs: open keeper+dupe thumbs from hash_thumbs2 (name = sha256(path)[:16].jpg), eyeball near-identical.
# Guard: allocation_v2.jsonl still 9,024 lines (wc -l), no source file deleted.
```
PENDING DECISION (spec §Output 3 last bullet, NOT executed): push verdicts to spine —
additive `duplicate_of text` + `dedupe_kind text` on visual_observations (LeeOSplus
jrmcvuqtvrgehrthwtjz), batched PATCH by original_path, verify-after: row count stays
8,488, patched count == flags rows whose path is a spine original_path. Do this only
after the flags verify.

## Doer C — flip_server regroup (CLOSED by its own report; I part-verified live)

Asked: regroup `/flip/<ref>` by billable-event window (±21d pad, nearest-edge wins),
dedupe collapse expander, sister links, unmatched-last, flat fallback + banner, £=0,
all existing flip behaviour intact, per spec §Output 4.
Lands: Mini A `~/image-plane/flip_server.py` LIVE on :8788 (doer-reported PID 88149;
re-check), laptop mirror committed 3af283c. Restart:
`ssh macminia@192.168.178.61 "cd ~/image-plane && nohup python3 flip_server.py > flip_server.log 2>&1 & disown"`.
I verified live MYSELF this window: 5 pages HTTP 200 / £ hits 0; "Install —
June–November 2021" single group; sister block names-first (0 bare-refcode leads);
screenshots f6-index.png, f6-flip-1124.png, f6-flip-301-14.png in this session's
scratchpad `/private/tmp/claude-501/-Users-Lee/0a2efb42-8f78-43e7-94ac-d6c586af223b/scratchpad/`
(copy them out if needed — scratchpad is session-scoped).
NOT yet verified by anyone on the live page: the 10 polished sister labels (doer A's
final rerun landed after my last page pull — server reads sister_refs.json per
request, no restart needed), and dedupe collapse on a REAL pair (zero collapsible
pairs existed pre-near-pass; only synthetic unit test so far).

Verify:
```
for p in "" "flip/1124-19" "flip/301-14" "flip/735-16" "flip/1051-19" "flip/1379-21"; do
  echo "/$p: $(curl -s -o /dev/null -w %{http_code} http://192.168.178.61:8788/$p) £=$(curl -s http://192.168.178.61:8788/$p | grep -c '£')"; done
curl -s http://192.168.178.61:8788/flip/1124-19 | grep -o "Install — June–November 2021" | wc -l   # 1
curl -s http://192.168.178.61:8788/flip/1124-19 | grep -o "25 Trinity Crescent — separate job number"  # polished label renders
curl -s http://192.168.178.61:8788/flip/301-14 | grep -c "refcode'>1336-21</span> &mdash;"          # 0 (bare-code lead)
curl -s http://192.168.178.61:8788/flip/1379-21 | python3 -c "import sys; h=sys.stdin.read(); i=h.find('Not yet matched to billing'); print('unmatched-last OK' if i>0 and 'group-header' not in h[i:] else 'CHECK MANUALLY')"
curl -s http://192.168.178.61:8788/flip/1051-19 | grep -c "No billing map yet for this roof"        # ≥1
curl -s -X POST http://192.168.178.61:8788/flip -H 'Content-Type: application/json' -d '{"id":"00000000-0000-0000-0000-000000000000","public":true}'  # 404 row not found
# After B verifies: pick a near-keeper's job_ref from the flags file, load its page, confirm "+N similar shots — tap to show" renders and expanded rows keep flip buttons.
```

## Then: merge + docs + the report to Lee

1. `git checkout main && git merge f6-repo-view && git push origin main` (add doer B's
   script mirror first). Update `CURRENT_STATE.md` (new top block) — include
   "Counted by:" lines from YOUR verification queries only.
2. Report to Lee, one glance: what changed on :8788, the three screenshots (retake
   fresh ones), the proof ladder (Loop Proof Gate — stages 1–4 claimable if your
   checks pass; stage 6 operator + stage 7 world = NOT YET, Lee has seen nothing),
   and the open items below.
3. Receipt rules: actual commit hashes; same-turn Counted by on every number;
   "no new Lee rulings this session" + the two Lee-questions below entered as
   QUESTIONS not rulings.

## Open Lee gates / questions (surface all in the report)
- **Facet question** (repos per roof/component/visit type vs filters): steel/straw
  delivered 11 Jul late eve, LEE HAS NOT RULED — present, do not build.
- **Victoria Rd**: is Victoria Park Bowls Club at 8a Victoria Rd, Portslade? If yes,
  677-16's 2017 membrane repairs join 1321-21's page as a same-site note.
- **Spine dedupe columns** (pending decision above) — execute or park, say which.
- GRA branch `roof-photo-timeline` merge + Vercel envs — unchanged, separate gate.
- Glengarry card — still blocked on Lee's record-view comparison ruling.

## OPEN DELIVERABLE — labelling speed-up explanation (Lee ordered it PRESENTED before
any labelling build; repo view was separately ordered and is built; present this text
with the report, then build NOTHING labelling-related until Lee picks):

**How labelling works today:** Lee dictates against numbered contact sheets; his words
land verbatim in knowledge_notes.jsonl as ground truth (388 rows now); one approved
spread pass copies his words onto closely-matching photos inside a safe bucket
(keyword match on existing descriptions, phase-gated, v2 words only, maintenance-sedum
is the only bucket spread so far). Machine labels never promote themselves (IP-L9).

**Low-hanging fruit, ranked by new-Lee-effort per label:**
1. **The billable-event grouping just built is the biggest lever and costs Lee
   nothing more** — one phase label per money window has already put
   install/care/repair phase context on every photo inside 612 windows (vs 8,488
   photos individually). Next step on the same lever: ONE Lee sentence per GROUP
   (612 possible dictations instead of 8,488) — the flip page groups are now the
   contact sheet.
2. **Harvest labels Lee already wrote elsewhere:** GRA visit records + graded
   condition fields (coverage %, moss, weeds, condition notes) are Lee-approved
   words tied to job+date — join to photos by date, they arrive as trusted labels,
   zero new dictation. (gra_stories.json already exists for the join.)
3. **Run the approved spread to the remaining v2 buckets** — blocked only on
   finishing the v2 alias map (map_v2_dryrun.py shows the retarget).
4. **Dedupe multiplies every label** — a label on a keeper covers its collapsed
   twins for free (near-dupe flags landing in this build).

**After the fruit:** model-ASSISTED proposals at scale — vision passes propose in
quarantine, grouped for one-tap yes/no on contact sheets, reliability measured
against Lee's existing hand labels (recall AND precision reported) BEFORE any bulk
run. Never auto-promote. The measurement design gets proposed to Lee first.

## Status at handover
- Doer A: landed (its report + my file-level spot checks earlier in window), FULL
  re-verify still owed by fresh window.
- Doer B: RUNNING (thumb batch ~19/41 at 19:19). If landed by the time you read
  this: **landed, unverified.**
- Doer C: landed; my live checks passed pre-label-polish; polished labels + real
  collapse UNVERIFIED.
- Branch f6-repo-view at 3af283c, unmerged. CURRENT_STATE.md not yet updated.
